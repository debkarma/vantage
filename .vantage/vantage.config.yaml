version: 1
app_port: 3000
record_port: 6789
noise:
  headers:
    - Date
    - ETag
    - X-Request-Id
    - Content-Length
  body_fields:
    - id
    - _id
    - createdAt
    - updatedAt
    - accessToken
    - refreshToken
scripts:
  pre_test: "echo [DB RESET] Running pre-test setup..."
  post_test: "echo [DB RESET] Running post-test cleanup..."
